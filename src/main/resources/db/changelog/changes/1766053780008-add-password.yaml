databaseChangeLog:
  - changeSet:
      id: 1766053780008-add-password
      author: cascade
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password_hash
                  type: VARCHAR(60)
                  constraints:
                    nullable: true
        - update:
            tableName: users
            columns:
              - column:
                  name: password_hash
                  value: "__PASSWORD_NOT_SET__"
            where: "password_hash IS NULL"
        - addNotNullConstraint:
            tableName: users
            columnName: password_hash
            columnDataType: VARCHAR(60)